name: mccp_1645
description: A Musical Co-creating Platform

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  animations: ^2.0.2 #好用的动画包
  get: ^4.6.1 #轻量级状态管理方案，同时集成路由管理和常用组件
  shared_preferences: ^2.0.12 #持久化存储
  http: ^0.13.4
  image_picker: ^0.8.4+5
  flutter_svg: ^1.0.3
  permission_handler: ^8.3.0
  sqflite: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true

  assets:
    - assets/avatars/
    - assets/covers/
    - assets/intro/
    - assets/auth/images/
    - assets/auth/icons/